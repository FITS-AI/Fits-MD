<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Fits" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@color/ash</item>
    </style>

    <style name="Theme.Fits" parent="Base.Theme.Fits" />

    <style name="Button.Lime" parent="ThemeOverlay.AppCompat">
        <item name="android:background">@drawable/lime_btn_rounded</item>
    </style>
</resources>